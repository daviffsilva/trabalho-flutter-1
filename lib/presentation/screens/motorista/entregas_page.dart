import 'package:flutter/material.dart';

class MotoristaEntregasPage extends StatelessWidget {
  const MotoristaEntregasPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Entregas Page - Motorista'),
    );
  }
} 