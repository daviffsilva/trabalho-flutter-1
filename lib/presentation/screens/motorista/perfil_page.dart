import 'package:flutter/material.dart';

class MotoristaPerfilPage extends StatelessWidget {
  const MotoristaPerfilPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Perfil Page - Motorista'),
    );
  }
} 