import 'package:flutter/material.dart';

class EntregasPage extends StatelessWidget {
  const EntregasPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Entregas Page - Cliente'),
    );
  }
} 